import PageHeader from "@/components/PageHeader";
import UploadForm from "@/components/form/UploadForm";

const EditPage = () => {
  return (
    <main>
      <PageHeader header="수정하기" />
      <UploadForm />
    </main>
  );
};

export default EditPage;
